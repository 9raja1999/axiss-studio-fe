import { Urbanist } from 'next/font/google';
import localFont from 'next/font/local';
import type { Metadata } from 'next';
import NextTopLoader from 'nextjs-toploader';

import './globals.css';

const urbanist = Urbanist({
  display: 'swap',
  variable: '--font-urbanist',
});

const gillSans = localFont({
  src: [
    {
      path: './fonts/gillSans/GillSans-Light.otf',
      weight: '300',
      style: 'normal',
    },
    {
      path: './fonts/gillSans/GillSans-Regular.otf',
      weight: '400',
      style: 'normal',
    },
    {
      path: './fonts/gillSans/GillSans-Medium.otf',
      weight: '500',
      style: 'normal',
    },
    {
      path: './fonts/gillSans/GillSans-Bold.otf',
      weight: '700',
      style: 'normal',
    },
  ],
  display: 'swap',
  variable: '--font-sans',
});

export const metadata: Metadata = {
  title: 'Create Next App',
  description: 'Generated by create next app',
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang='en'>
      <body className={`${gillSans.variable} ${urbanist.variable} antialiased`}>
        <NextTopLoader color='#f95e27' zIndex={1600} showSpinner={false} />
        {children}
      </body>
    </html>
  );
}
